@extends('layouts.dashboard')
@section('content')<section class="p-5 lg:p-10"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-amber-600">PPDB Online</p><h1 class="mt-2 font-display text-4xl text-emerald-950">Pendaftar masuk</h1></div><a href="{{ route('ppdb.register') }}" target="_blank" class="rounded-xl bg-amber-500 px-4 py-2.5 text-sm font-bold text-white"><i class="fa-solid fa-arrow-up-right-from-square mr-2"></i>Lihat formulir</a></div><form method="get" class="dashboard-card mt-8 flex flex-wrap gap-2 rounded-2xl bg-white p-4"><input name="q" value="{{ request('q') }}" placeholder="Nama, nomor, telepon" class="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><select name="unit" class="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><option value="">Semua unit</option><option value="mi" @selected(request('unit') === 'mi')>MI</option><option value="mts" @selected(request('unit') === 'mts')>MTs</option></select><select name="status" class="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><option value="">Semua status</option>@foreach(['baru','diverifikasi','diterima','ditolak'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="rounded-xl bg-emerald-800 px-4 py-2.5 text-sm font-bold text-white">Filter</button></form><div class="mt-5 space-y-4">@forelse($applications as $application)<div class="dashboard-card rounded-3xl bg-white p-5 lg:p-7"><div class="flex flex-wrap items-start justify-between gap-4"><div><span class="font-mono text-xs font-bold text-amber-600">{{ $application->nomor_pendaftaran }}</span><h2 class="mt-2 text-xl font-bold text-emerald-950">{{ $application->nama_lengkap }}</h2><p class="mt-1 text-sm text-slate-500">{{ strtoupper($application->unit) }} · Kelas {{ $application->kelas_tujuan }} · Wali: {{ $application->nama_wali }}</p><p class="mt-3 text-sm text-slate-600">{{ $application->telepon }} @if($application->email) · {{ $application->email }} @endif</p></div><form method="post" action="{{ route('dashboard.ppdb.update', $application) }}" class="flex flex-wrap gap-2">@csrf @method('put')<select name="status" class="rounded-xl border border-slate-200 px-3 py-2 text-sm">@foreach(['baru','diverifikasi','diterima','ditolak'] as $status)<option value="{{ $status }}" @selected($application->status === $status)>{{ ucfirst($status) }}</option>@endforeach</select><input name="catatan" value="{{ $application->catatan }}" placeholder="Catatan" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><button class="rounded-xl bg-emerald-800 px-3 py-2 text-sm font-bold text-white">Simpan</button></form></div><p class="mt-4 text-sm text-slate-500">{{ $application->alamat }}</p></div>@empty<div class="dashboard-card rounded-3xl bg-white p-8 text-center text-sm text-slate-500">Belum ada pendaftar.</div>@endforelse</div><div class="mt-5">{{ $applications->links() }}</div></section>@endsection