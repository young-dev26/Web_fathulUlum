@extends('layouts.dashboard')
@section('content')<section class="p-5 lg:p-10"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-amber-600">Laporan</p><h1 class="mt-2 font-display text-4xl text-emerald-950">Rekap absensi</h1></div><form class="dashboard-card flex flex-wrap gap-2 rounded-2xl bg-white p-4" method="get"><input type="date" name="tanggal" value="{{ $date }}" class="rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm"><input name="q" value="{{ request('q') }}" placeholder="Nama / NISN" class="w-36 rounded-xl border border-slate-200 px-3 py-2 text-sm"><select name="unit" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="">Semua unit</option><option value="mi" @selected(request('unit') === 'mi')>MI</option><option value="mts" @selected(request('unit') === 'mts')>MTs</option></select><select name="kelas" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="">Semua kelas</option>@for($class = 1; $class <= 9; $class++)<option value="{{ $class }}" @selected((string) request('kelas') === (string) $class)>Kelas {{ $class }}</option>@endfor</select><select name="status" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="">Semua status</option>@foreach(['Hadir','Izin','Sakit','Alpha'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ $status }}</option>@endforeach</select><select name="metode" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="">Semua metode</option><option value="QR_Scan" @selected(request('metode') === 'QR_Scan')>QR Scan</option><option value="Manual" @selected(request('metode') === 'Manual')>Manual</option></select><button class="rounded-xl bg-emerald-800 px-4 py-2 text-sm font-bold text-white">Filter</button><a href="{{ route('dashboard.attendances.index') }}" class="rounded-xl px-3 py-2 text-sm font-bold text-slate-500 hover:bg-slate-100">Reset</a></form></div><div class="mt-8 overflow-x-auto rounded-xl bg-white shadow-sm"><table class="w-full min-w-[680px] text-left text-sm"><thead class="border-b border-slate-100 text-xs uppercase tracking-wider text-slate-400"><tr><th class="p-5">Siswa</th><th>Unit / Kelas</th><th>Jam</th><th>Status</th><th>Metode</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($attendances as $attendance)<tr><td class="p-5 font-bold text-emerald-950">{{ $attendance->student->nama_lengkap }}</td><td>{{ strtoupper($attendance->student->unit) }} / {{ $attendance->student->kelas }}</td><td>{{ $attendance->jam_masuk ?? '-' }}</td><td><span class="rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-700">{{ $attendance->status }}</span></td><td>{{ $attendance->metode }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-400">Belum ada data absensi pada tanggal ini.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $attendances->links() }}</div></section>@endsection
