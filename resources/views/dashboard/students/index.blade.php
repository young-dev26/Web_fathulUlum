@extends('layouts.dashboard')
@section('content')<section class="p-5 lg:p-10"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-amber-600">Master data</p><h1 class="mt-2 font-display text-4xl text-emerald-950">Data siswa</h1></div><a href="{{ route('dashboard.students.create') }}" class="rounded-lg bg-amber-500 px-4 py-2.5 text-sm font-bold text-white"><i class="fa-solid fa-plus mr-2"></i>Tambah siswa</a></div><form class="dashboard-card mt-8 flex flex-wrap items-center gap-2 rounded-2xl bg-white p-4" method="get"><div class="relative min-w-[230px] flex-1"><i class="fa-solid fa-magnifying-glass absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"></i><input name="q" value="{{ request('q') }}" placeholder="Cari nama atau NISN..." class="w-full rounded-xl border border-slate-200 bg-slate-50 py-2.5 pl-9 pr-3 text-sm"></div><select name="unit" class="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm"><option value="">Semua unit</option><option value="mi" @selected(request('unit') === 'mi')>MI</option><option value="mts" @selected(request('unit') === 'mts')>MTs</option></select><input name="kelas" value="{{ request('kelas') }}" type="number" min="1" max="9" placeholder="Kelas" class="w-24 rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><select name="status" class="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm"><option value="">Semua status</option><option value="aktif" @selected(request('status') === 'aktif')>Aktif</option><option value="nonaktif" @selected(request('status') === 'nonaktif')>Nonaktif</option></select><button class="rounded-xl bg-emerald-800 px-4 py-2.5 text-sm font-bold text-white"><i class="fa-solid fa-filter mr-2 text-amber-300"></i>Filter</button><a href="{{ route('dashboard.students.index') }}" class="rounded-xl px-3 py-2.5 text-sm font-bold text-slate-500 hover:bg-slate-100">Reset</a></form><div class="mt-5 overflow-x-auto rounded-xl bg-white shadow-sm"><table class="w-full min-w-[700px] text-left text-sm"><thead class="border-b border-slate-100 text-xs uppercase tracking-wider text-slate-400"><tr><th class="p-5">Nama</th><th>NISN</th><th>Unit</th><th>Kelas</th><th>Status</th><th>Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($students as $student)<tr><td class="p-5 font-bold text-emerald-950">{{ $student->nama_lengkap }}</td><td>{{ $student->nisn }}</td><td>{{ strtoupper($student->unit) }}</td><td>{{ $student->kelas }}</td><td>{{ $student->status_aktif ? 'Aktif' : 'Nonaktif' }}</td><td><a class="font-bold text-emerald-700" href="{{ route('dashboard.students.card', $student) }}">Kartu QR</a></td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-slate-400">Belum ada siswa.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $students->links() }}</div></section>@endsection
