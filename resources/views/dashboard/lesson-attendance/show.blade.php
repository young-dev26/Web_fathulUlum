@extends('layouts.dashboard')
@section('content')
<section class="p-5 lg:p-10"><a href="{{ route('dashboard.lesson-attendance.index') }}" class="text-sm font-bold text-emerald-700"><i class="fa-solid fa-arrow-left mr-2"></i>Kembali ke sesi</a><div class="mt-5 rounded-2xl bg-emerald-800 p-6 text-white"><p class="text-xs font-bold uppercase tracking-wider text-amber-300">{{ $session->session_date->format('d/m/Y') }} · Jam ke-{{ $session->schedule->slot->period_number }}</p><h1 class="mt-2 font-display text-3xl">{{ $session->schedule->assignment->subject->name }}</h1><p class="mt-1 text-sm text-emerald-100/70">{{ $session->schedule->assignment->rombel->name }} · {{ $session->schedule->slot->starts_at }}-{{ $session->schedule->slot->ends_at }}</p></div>@if($session->status === 'open')<form action="{{ route('dashboard.lesson-attendance.store', $session) }}" method="post" class="mt-6 rounded-2xl bg-white p-5 shadow-sm">@csrf<div class="divide-y divide-slate-100">@foreach($session->schedule->assignment->rombel->students as $student)@php($current = $session->attendances->firstWhere('student_id', $student->id)?->status ?? 'Hadir')<div class="flex flex-wrap items-center justify-between gap-3 py-4"><div><p class="font-bold text-emerald-950">{{ $student->nama_lengkap }}</p><p class="text-xs text-slate-400">{{ $student->nis ?? $student->nisn }}</p></div><select name="attendance[{{ $student->id }}]" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option @selected($current === 'Hadir')>Hadir</option><option @selected($current === 'Izin')>Izin</option><option @selected($current === 'Sakit')>Sakit</option><option @selected($current === 'Alpha')>Alpha</option></select></div>@endforeach</div><button class="mt-5 rounded-xl bg-emerald-800 px-5 py-3 text-sm font-bold text-white"><i class="fa-solid fa-lock mr-2"></i>Simpan dan tutup sesi</button></form>@else<div class="mt-6 overflow-x-auto rounded-2xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b border-slate-100 text-xs uppercase text-slate-400"><tr><th class="p-5">Siswa</th><th>Status</th><th>Waktu</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($session->attendances as $attendance)<tr><td class="p-5 font-semibold">{{ $attendance->student->nama_lengkap }}</td><td>{{ $attendance->status }}</td><td>{{ $attendance->check_in_at ?? '-' }}</td></tr>@endforeach</tbody></table></div>@endif</section>
@endsection