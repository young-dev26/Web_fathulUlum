@extends('layouts.dashboard')
@section('content')
<section class="p-5 lg:p-10"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-amber-600">Jadwal pelajaran</p><h1 class="mt-2 font-display text-4xl text-emerald-950">Absensi per sesi</h1><p class="mt-2 text-sm text-slate-500">Buka sesi sesuai jadwal, lalu isi kehadiran seluruh siswa rombel.</p></div><form method="get"><input type="date" name="tanggal" value="{{ request('tanggal', now()->toDateString()) }}" class="rounded-xl border border-slate-200 px-4 py-3 text-sm"><button class="ml-2 rounded-xl bg-emerald-800 px-4 py-3 text-sm font-bold text-white">Tampilkan</button></form></div>
<div class="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($sessions as $session)<article class="dashboard-card rounded-2xl bg-white p-5"><div class="flex items-start justify-between gap-3"><span class="rounded-full bg-emerald-100 px-3 py-1 text-xs font-bold text-emerald-800">Jam ke-{{ $session->schedule->slot->period_number }}</span><span class="text-xs text-slate-400">{{ $session->status === 'closed' ? 'Selesai' : 'Terbuka' }}</span></div><h2 class="mt-5 text-lg font-bold text-emerald-950">{{ $session->schedule->assignment->subject->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $session->schedule->assignment->rombel->name }} · {{ $session->schedule->slot->starts_at }}-{{ $session->schedule->slot->ends_at }}</p>@if($session->status === 'open')<a href="{{ route('dashboard.lesson-attendance.show', $session) }}" class="mt-5 inline-flex rounded-xl bg-amber-500 px-4 py-3 text-sm font-bold text-white">Isi absensi <i class="fa-solid fa-arrow-right ml-2"></i></a>@else<a href="{{ route('dashboard.lesson-attendance.show', $session) }}" class="mt-5 inline-flex rounded-xl bg-slate-100 px-4 py-3 text-sm font-bold text-slate-700">Lihat rekap</a>@endif</article>@empty<div class="rounded-2xl bg-white p-10 text-center text-sm text-slate-500 md:col-span-2 xl:col-span-3">Belum ada sesi pelajaran untuk tanggal ini.</div>@endforelse</div></section>
@endsection