@extends('layouts.dashboard')
@section('content')
<section class="p-5 lg:p-10"><p class="text-xs font-bold uppercase tracking-[.18em] text-amber-600">Riwayat akademik</p><h1 class="mt-2 font-display text-4xl text-emerald-950">Absensi setiap pelajaran</h1><p class="mt-2 text-sm text-slate-500">Catatan kehadiran berdasarkan sesi jadwal yang telah ditutup.</p><div class="mt-8 overflow-x-auto rounded-2xl bg-white shadow-sm"><table class="w-full min-w-[650px] text-left text-sm"><thead class="border-b border-slate-100 text-xs uppercase tracking-wider text-slate-400"><tr><th class="p-5">Tanggal</th><th>Pelajaran</th><th>Rombel</th><th>Siswa</th><th>Status</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($records as $record)<tr><td class="p-5">{{ $record->session->session_date->format('d/m/Y') }}</td><td class="font-semibold">{{ $record->session->schedule->assignment->subject->name }}</td><td>{{ $record->session->schedule->assignment->rombel->name }}</td><td>{{ $record->student->nama_lengkap }}</td><td>{{ $record->status }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-400">Belum ada riwayat absensi pelajaran.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $records->links() }}</div></section>
@endsection