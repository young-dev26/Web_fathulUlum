@extends('layouts.dashboard')

@section('content')
    <section class="p-5 lg:p-10">
        <p class="text-xs font-bold uppercase tracking-[.18em] text-amber-600">Manajemen akun</p><h1 class="mt-2 font-display text-4xl text-emerald-950">Orang tua</h1><p class="mt-2 text-sm text-slate-500">Buat akun dan hubungkan satu atau beberapa anak.</p>
        <form method="post" action="{{ route('dashboard.parents.store') }}" class="dashboard-card mt-8 grid gap-3 rounded-2xl bg-white p-5 md:grid-cols-2 lg:grid-cols-3">@csrf<input name="nama_lengkap" required placeholder="Nama lengkap wali" class="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><input name="email" type="email" required placeholder="Email login" class="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><input name="password" type="password" required minlength="8" placeholder="Password minimal 8 karakter" class="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><input name="hubungan" placeholder="Hubungan, contoh: Ayah" class="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><input name="telepon" placeholder="Nomor telepon" class="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><select name="student_ids[]" multiple class="min-h-24 rounded-xl border border-slate-200 px-3 py-2.5 text-sm">@foreach($students as $student)<option value="{{ $student->id }}">{{ $student->nama_lengkap }} · {{ strtoupper($student->unit) }} · {{ $student->nisn }}</option>@endforeach</select><button class="rounded-xl bg-emerald-800 px-4 py-2.5 text-sm font-bold text-white md:col-span-2 lg:col-span-3">Buat akun orang tua</button></form>
        <div class="mt-6 grid gap-4 xl:grid-cols-2">@forelse($parents as $parent)<article class="dashboard-card rounded-2xl bg-white p-5"><div class="flex items-start justify-between gap-4"><div><h2 class="font-bold text-emerald-950">{{ $parent->nama_lengkap }}</h2><p class="mt-1 text-sm text-slate-500">{{ $parent->email }} · {{ $parent->hubungan ?: 'Wali' }}</p></div><i class="fa-solid fa-user-group text-xl text-emerald-700"></i></div><form method="post" action="{{ route('dashboard.parents.update', $parent) }}" class="mt-5 grid gap-3 sm:grid-cols-2">@csrf @method('put')<input name="nama_lengkap" value="{{ $parent->nama_lengkap }}" required class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><input name="email" type="email" value="{{ $parent->email }}" required class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><input name="password" type="password" minlength="8" placeholder="Password baru (opsional)" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><input name="hubungan" value="{{ $parent->hubungan }}" placeholder="Hubungan" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><input name="telepon" value="{{ $parent->telepon }}" placeholder="Telepon" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><select name="student_ids[]" multiple class="min-h-24 rounded-xl border border-slate-200 px-3 py-2 text-sm sm:col-span-2">@foreach($students as $student)<option value="{{ $student->id }}" @selected($parent->children->contains($student->id))>{{ $student->nama_lengkap }} · {{ strtoupper($student->unit) }}</option>@endforeach</select><button class="rounded-xl bg-amber-500 px-4 py-2 text-sm font-bold text-white sm:col-span-2">Simpan perubahan</button></form></article>@empty<div class="dashboard-card rounded-2xl bg-white p-8 text-center text-sm text-slate-500 xl:col-span-2">Belum ada akun orang tua.</div>@endforelse</div>
    </section>
@endsection