@extends('layouts.dashboard')
@section('content')<section class="p-5 lg:p-10"><p class="text-xs font-bold uppercase tracking-[.18em] text-amber-600">Portal siswa</p><h1 class="mt-2 font-display text-4xl text-emerald-950">{{ $student->nama_lengkap }}</h1><p class="mt-2 text-sm text-slate-500">{{ strtoupper($student->unit) }} · Kelas {{ $student->kelas }} · Riwayat absensi</p><form class="dashboard-card mt-7 flex flex-wrap gap-2 rounded-2xl bg-white p-4" method="get"><select name="status" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><option value="">Semua status</option>@foreach(['Hadir','Izin','Sakit','Alpha'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ $status }}</option>@endforeach</select><input type="date" name="tanggal_mulai" value="{{ request('tanggal_mulai') }}" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><input type="date" name="tanggal_selesai" value="{{ request('tanggal_selesai') }}" class="rounded-xl border border-slate-200 px-3 py-2 text-sm"><button class="rounded-xl bg-emerald-800 px-4 py-2 text-sm font-bold text-white">Filter</button><a href="{{ route('dashboard.student.attendance') }}" class="rounded-xl px-3 py-2 text-sm font-bold text-slate-500 hover:bg-slate-100">Reset</a></form><div class="mt-8 overflow-x-auto rounded-xl bg-white shadow-sm"><table class="w-full min-w-[500px] text-left text-sm"><thead class="border-b border-slate-100 text-xs uppercase tracking-wider text-slate-400"><tr><th class="p-5">Tanggal</th><th>Jam masuk</th><th>Status</th><th>Metode</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($attendances as $attendance)<tr><td class="p-5">{{ $attendance->tanggal->format('d/m/Y') }}</td><td>{{ $attendance->jam_masuk ?? '-' }}</td><td>{{ $attendance->status }}</td><td>{{ $attendance->metode }}</td></tr>@empty<tr><td colspan="4" class="p-10 text-center text-slate-400">Belum ada riwayat absensi.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $attendances->links() }}</div></section>@endsection
