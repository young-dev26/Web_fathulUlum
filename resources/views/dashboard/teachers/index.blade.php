@extends('layouts.dashboard')
@section('content')<section class="p-5 lg:p-10"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-amber-600">Master data</p><h1 class="mt-2 font-display text-4xl text-emerald-950">Data guru</h1></div><a href="{{ route('dashboard.teachers.create') }}" class="rounded-xl bg-amber-500 px-4 py-2.5 text-sm font-bold text-white"><i class="fa-solid fa-plus mr-2"></i>Tambah guru</a></div><form method="get" class="dashboard-card mt-8 flex flex-wrap gap-2 rounded-2xl bg-white p-4"><input name="q" value="{{ request('q') }}" placeholder="Nama, email, atau NIP/NUPTK" class="min-w-[240px] flex-1 rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><select name="unit" class="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><option value="">Semua unit</option>@foreach(['mi','mts','yayasan'] as $unit)<option value="{{ $unit }}" @selected(request('unit') === $unit)>{{ strtoupper($unit) }}</option>@endforeach</select><button class="rounded-xl bg-emerald-800 px-4 py-2.5 text-sm font-bold text-white">Filter</button><a href="{{ route('dashboard.teachers.index') }}" class="rounded-xl px-3 py-2.5 text-sm font-bold text-slate-500">Reset</a></form><div class="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($teachers as $teacher)<article class="dashboard-card rounded-3xl bg-white p-5"><div class="flex items-start justify-between gap-3"><span class="grid h-11 w-11 place-items-center rounded-2xl bg-emerald-100 font-display text-xl text-emerald-800">{{ strtoupper(substr($teacher->nama_lengkap, 0, 1)) }}</span><span class="rounded-full bg-amber-100 px-2.5 py-1 text-xs font-bold text-amber-800">{{ strtoupper($teacher->unit) }}</span></div><h2 class="mt-5 text-lg font-bold text-emerald-950">{{ $teacher->nama_lengkap }}</h2><p class="mt-1 text-sm text-slate-500">{{ $teacher->mata_pelajaran ?: 'Tenaga pendidik' }}</p><p class="mt-4 text-xs text-slate-400">{{ $teacher->email }}<br>{{ $teacher->nip_nuptk ?: 'NIP/NUPTK belum diisi' }}</p><div class="mt-5 flex gap-3"><a href="{{ route('dashboard.teachers.edit', $teacher) }}" class="text-sm font-bold text-emerald-700">Edit</a><form method="post" action="{{ route('dashboard.teachers.destroy', $teacher) }}">@csrf @method('delete')<button class="text-sm font-bold text-rose-600">Hapus</button></form></div></article>@empty<div class="dashboard-card rounded-3xl bg-white p-8 text-center text-sm text-slate-500 md:col-span-2 xl:col-span-3">Belum ada data guru.</div>@endforelse</div><div class="mt-5">{{ $teachers->links() }}</div></section>@endsection